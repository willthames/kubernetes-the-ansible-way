# inventory.config file in YAML format
plugin: generator
strict: False
hosts:
    name: "kubernetes-{{ environment }}-runner"
    parents:
      - name: "kubernetes"
      - name: "{{ environment }}"
        vars:
          env: "{{ environment }}"
      - name: runner
layers:
    environment:
        - staging
